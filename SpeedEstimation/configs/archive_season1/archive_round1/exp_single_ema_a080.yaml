# =============================================================================
# EMA Post-Filter α=0.80 (강한 스무딩, 응답 지연 가능)
# baseline 대비 변경: 03_eval.ema_alpha → 0.80 (추론 전용)
# 학습은 baseline과 100% 동일
# =============================================================================

03_eval:
  ema_alpha: 0.80

exp_name: "exp_single_ema_a080"
