# =============================================================================
# EMA Post-Filter α=0.95 (약한 스무딩, 응답성 유지)
# baseline 대비 변경: 03_eval.ema_alpha → 0.95
# =============================================================================

03_eval:
  ema_alpha: 0.95

exp_name: "exp_single_ema_a095"
